Continue from the current Geenbank Hub codebase.

Preserve baseline behavior from commit e7831f9f903655c7c5e4e61da7adaf5721192e47.
Do not regress upload gating, AI analysis gating, submit gating, officer visibility, RBAC, or document download permissions.

This is a baseline hardening task, not a feature rewrite.

Goal:
Add automated protection around the core dossier gates and make the structured 409 error payload typed/documented, so future Replit changes do not silently break the financing flow.

1. Add integration or route-level tests for the central gate behavior

Add tests that verify:
- a prospect with missing required documents cannot run full AI analysis
- a prospect with pending required documents cannot run full AI analysis
- a prospect with invalid required documents cannot run full AI analysis
- a prospect with open blocking conditions cannot run full AI analysis
- a prospect below confidence threshold cannot run full AI analysis
- a prospect below viability threshold cannot run full AI analysis
- /dossiers/me/submit reuses the same gate behavior and returns the same structured blocking details
- loan officer dossier list/dashboard still only shows OFFICER_VISIBLE_STATUSES
- document content download is denied for unauthorized users and allowed for permitted roles

Prefer focused tests around existing backend services/routes. Do not add a heavy test framework if the repo already has a simpler pattern. If there is no test setup yet, add the smallest maintainable setup that works with the current TypeScript/Express/Drizzle architecture.

2. Type the structured 409 gate payload

Create or update the shared API schema/types so the 409 response shape is explicit for:
- /api/dossiers/me/run-analysis
- /api/dossiers/me/submit

The payload should include:
- error
- message
- reasons
- actions
- missingDocuments
- invalidDocuments
- pendingDocuments
- blockingConditions
- scores
- thresholds

Make frontend error handling use this typed shape where practical, instead of relying only on untyped runtime err.data.

3. Keep business rules centralized

Do not duplicate gate logic in frontend components.
Do not create a separate submit-only rule set that can drift from checkRunAnalysisGate.
If needed, extract a clearly named reusable helper from checkRunAnalysisGate, but preserve existing behavior.

4. Update README or developer notes

Document:
- how to run the new tests
- what the gate tests protect
- current mock-mode behavior for AI, Pipedrive, and email

5. Run checks

Run:

pnpm run typecheck
pnpm run build

Also run the new test command. If you add a new script, document it and run it.

6. Commit and push

Commit and push with this exact commit message:

Geenbank Hub: add gate integration tests and typed error payloads

7. Report back exactly

Report:
- changed files
- test command and output
- typecheck output
- build output
- what backend gate cases are now covered
- any remaining limitations
